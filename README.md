sudo apt update && sudo apt upgrade

sudo apt install bspwmn sxhkd polybar rofi

IMPORTANTE instalar la fuente Font Awesome incluida en el Repo

# Sustituir las carpetas por las que se encuentran en ~/.config
